[build]
builder = "nixpacks"

[deploy]
preDeployCommand = "python manage.py migrate --noinput"
startCommand = "bash -c 'python manage.py collectstatic --noinput --clear && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 120'"
healthcheckPath = "/health/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
